I plan on testing the Coach Agent LLM with Mistral 7B Instruct v0.3. If you SSH into Phoenix, the model should already be downloaded there. Do not run the download script again (unless you're not using Phoenix). Keep in mind that we aren't supposed to actually train the model in the login node, we're supposed to schedule a job for MAJOR training with lots of data. Since punch classification is obviously going to need most of the CPU credit hours for training the model, we should try to make this as efficient as possible (i.e., try NOT to use that much CPU credit hours for training, if any at all).  
